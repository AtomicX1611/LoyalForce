# ============================================================
# LoyalForce Backend — Production Dependencies
# ============================================================

# --- Web Framework ---
fastapi==0.111.0
uvicorn[standard]==0.29.0

# --- Database (async) ---
motor==3.4.0
pymongo==4.7.2          # used by seed_db.py (sync) and motor internally

# --- Authentication ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Config & Validation ---
pydantic-settings==2.2.1
python-dotenv==1.0.1

# --- ML Inference ---
# Versions below have pre-built Python 3.13 wheels on PyPI.
joblib==1.4.2
scikit-learn==1.6.1
numpy==2.2.6
pandas==2.2.3
